{
  "permissions": {
    "deny": [
      "Read(build/**)",
      "Read(**/*.o)",
      "Read(**/*.a)",
      "Read(**/*.so)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(cpp|hpp|h|cxx|cc|hxx|inl)$' && clang-format -i \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}